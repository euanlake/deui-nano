# Archived Trees

This directory keeps large or legacy code that is not part of the active ESP-IDF firmware target.

- `arduino-prototype/` is the previous Arduino-oriented prototype tree formerly stored at repo-root `main/`.
- `lvgl-v8/` is a vendored LVGL source snapshot formerly stored at `lib/lvgl/`.

The canonical, actively maintained firmware remains under `firmware/esp32/`.
